import React from 'react'
import ReactDOM from 'react-dom/client'
import './index.css'
import Wireframes from './Wireframes.tsx'

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <Wireframes />
  </React.StrictMode>,
)
